# Test Output

This directory stores CSV files generated by `parser_test.py`.

Committed snapshot fixtures live one directory up in `test/`:

- `test/expected_stdout.txt`
- `test/expected_output.csv`

Everything else in this directory is gitignored so test runs do not overwrite the script's normal outputs.
